#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, k;
    int count = 0;
    cin >> n >> k;
    int arr[n];
    for(int i=0;i<8;i++){
        cin>>arr[i];
        cout<<arr[i];
    }
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> arr[i];
    //     if (arr[i] >= arr[k] && arr[i] > 0)
    //     {
    //         count++;
    //     }
    // }
    // cout << count << endl;
}