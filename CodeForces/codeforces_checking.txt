#include<bits/stdc++.h>
using namespace std;



int main(){
    #ifndef ONLINE_JUDGE
        freopen("input.txt","r",stdin);
        freopen("output.txt","w",stdout);
    #endif
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int num;
    cin>>num;
    string s="codeforces"

    for(int i=0;i<num;i++){
        char a;
        cin>>a;
        int count;
        for(auto x:s){
            if(x==a){
                count++;
            }
        }
        if(count>0){
            cout<<"yes";

        }else{
            cout<<"no";
        }

    }
    












    
    return 0;
    

}            