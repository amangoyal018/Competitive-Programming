#include <bits/stdc++.h>

using namespace std;

int main()
{

#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin>>t;

    while(t--){
        int n,m;
        cin>>n>>m;

        vector<int> v1;
        for(int i=0;i<n;i++){
            int k;
            cin>>k;
            v1.push_back(k);
        }
        while(m--){
            int a,b,c;
            cin>>a>>b>>c;
            for(int i=0;i<n;i++){
                int k=v1[0];
                
            }
        }
    }



}
